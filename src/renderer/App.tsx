import { useCallback, useEffect, useMemo, useRef, useState } from "react";
import { Actions, DockLocation, Layout, Model, type IJsonModel, type TabNode } from "flexlayout-react";
import "flexlayout-react/style/dark.css";
import { createNode, emptyMap, flatten, insertNode, type IndexedNode, type MapDocument, type MapNode, nodePassesTagFilter, removeNode, updateNode } from "./model";
import { NodeEditor } from "./NodeEditor";
import { AssistantPanel } from "./AssistantPanel";

type PanelId = "graph" | "inspector" | "outline" | "search" | "activity" | "assistant";
const PANEL_IDS: PanelId[] = ["graph", "inspector", "outline", "search", "activity", "assistant"];
const panelLabel: Record<PanelId, string> = { graph: "Graph", inspector: "Inspector", outline: "Outline", search: "Search", activity: "Activity", assistant: "AI Assistant" };

const defaultWorkspace: IJsonModel = { global: { splitterSize: 5, tabEnableClose: true, tabEnableRename: false, tabEnablePopout: true, tabEnablePopoutIcon: true, tabSetEnableMaximize: true }, borders: [], layout: { type: "row", children: [
  { type: "tabset", id: "graph-tabset", weight: 70, children: [{ type: "tab", id: "panel-graph", name: "Graph", component: "graph", enablePopout: true }] },
  { type: "row", weight: 30, children: [
    { type: "tabset", id: "inspector-tabset", weight: 62, children: [{ type: "tab", id: "panel-inspector", name: "Inspector", component: "inspector", enablePopout: true }] },
    { type: "tabset", id: "utility-tabset", weight: 38, children: [
      { type: "tab", id: "panel-outline", name: "Outline", component: "outline", enablePopout: true },
      { type: "tab", id: "panel-search", name: "Search", component: "search", enablePopout: true },
      { type: "tab", id: "panel-activity", name: "Activity", component: "activity", enablePopout: true },
      { type: "tab", id: "panel-assistant", name: "AI Assistant", component: "assistant", enablePopout: true },
    ] },
  ] },
] } };

function workspaceModel(saved?: unknown): Model { try { return Model.fromJson(saved && typeof saved === "object" ? saved as IJsonModel : defaultWorkspace); } catch { return Model.fromJson(defaultWorkspace); } }

function ensureGraph(model: Model): Model {
  if (model.getNodeById("panel-graph")) { model.doAction(Actions.selectTab("panel-graph")); return model; }
  const destination = model.getActiveTabset() ?? model.getFirstTabSet(); if (!destination) return Model.fromJson(defaultWorkspace);
  model.doAction(Actions.addNode({ type: "tab", id: "panel-graph", name: "Graph", component: "graph", enablePopout: true }, destination.getId(), DockLocation.CENTER, 0, true)); return model;
}

function visibleNodes(document: MapDocument): IndexedNode[] {
  const expanded = new Set(document.view.expanded); const result: IndexedNode[] = [];
  const visit = (nodes: MapNode[], parentId?: string, depth = 0) => { for (const node of nodes) { result.push({ ...node, parentId, depth }); if (expanded.has(node.id)) visit(node.children, node.id, depth + 1); } };
  visit(document.nodes); return result;
}

function nodeRadii(nodes: IndexedNode[]): Map<string, number> {
  const incoming = new Map<string, number>(); for (const node of nodes) for (const link of node.links) incoming.set(link.target, (incoming.get(link.target) ?? 0) + 1);
  return new Map(nodes.map((node) => { const text = node.title.length + node.summary.length + (node.rationale?.length ?? 0); const score = text / 75 + node.children.length * 3.4 + (node.links.length + (incoming.get(node.id) ?? 0)) * 2.2; return [node.id, Math.min(110, 31 + Math.sqrt(Math.max(1, score)) * 10)]; }));
}

function wrapTitle(text: string, radius: number): string[] {
  const max = Math.max(8, Math.floor(radius / 4.3)); const words = text.split(/\s+/); const lines: string[] = [];
  for (const word of words) { const current = lines.at(-1); if (!current || current.length + word.length + 1 > max) lines.push(word); else lines[lines.length - 1] = `${current} ${word}`; }
  const limit = radius < 48 ? 2 : 3; if (lines.length > limit) { lines.length = limit; lines[limit - 1] = `${lines[limit - 1].slice(0, Math.max(3, max - 1))}…`; } return lines;
}

function tagHue(tag: string): number { let hash = 2166136261; for (const character of tag) { hash ^= character.charCodeAt(0); hash = Math.imul(hash, 16777619); } hash ^= hash >>> 16; hash = Math.imul(hash, 0x45d9f3b); hash ^= hash >>> 16; return (hash >>> 0) % 360; }
function nodePalette(node: IndexedNode): { fill: string; stroke: string; glow: string } {
  const hue = tagHue(node.main_tag); const depthDarkening = Math.min(34, node.depth * 9);
  return { fill: `hsl(${hue} 42% ${55 - depthDarkening}% / 0.94)`, stroke: `hsl(${hue} 74% ${91 - depthDarkening * 0.68}%)`, glow: `drop-shadow(0 0 7px hsl(${hue} 72% 62% / 0.42))` };
}

function fitGraph(placed: Array<{ node: IndexedNode; point: [number, number] }>, radii: Map<string, number>): { zoom: number; viewport: [number, number] } {
  if (!placed.length) return { zoom: 0.7, viewport: [0, 0] }; let left = Infinity, top = Infinity, right = -Infinity, bottom = -Infinity;
  for (const { node, point } of placed) { const radius = radii.get(node.id) ?? 40; left = Math.min(left, point[0] - radius); right = Math.max(right, point[0] + radius); top = Math.min(top, point[1] - radius); bottom = Math.max(bottom, point[1] + radius); }
  const zoom = Math.max(0.2, Math.min(1.15, 1080 / Math.max(1, right - left), 680 / Math.max(1, bottom - top))); const centerX = (left + right) / 2, centerY = (top + bottom) / 2;
  return { zoom, viewport: [-zoom * (centerX - 600), -zoom * (centerY - 400)] };
}

function clientToViewBox(svg: SVGSVGElement, clientX: number, clientY: number): [number, number] {
  const inverse = svg.getScreenCTM()?.inverse(); if (!inverse) return [clientX, clientY];
  const point = new DOMPoint(clientX, clientY).matrixTransform(inverse); return [point.x, point.y];
}

function screenDeltaToWorld(svg: SVGSVGElement, fromX: number, fromY: number, toX: number, toY: number, zoom: number): [number, number] {
  const from = clientToViewBox(svg, fromX, fromY), to = clientToViewBox(svg, toX, toY); return [(to[0] - from[0]) / zoom, (to[1] - from[1]) / zoom];
}

function HoverTooltip({ node, x, y, hostWindow }: { node: IndexedNode; x: number; y: number; hostWindow: Window }) {
  return <aside className="node-tooltip hover-tooltip" style={{ left: Math.max(12, Math.min(x + 18, hostWindow.innerWidth - 382)), top: Math.max(12, Math.min(y + 18, hostWindow.innerHeight - 120)) }}><div className="node-tooltip-heading"><span className="node-tooltip-swatch" style={{ background: nodePalette(node).stroke }} /><div><strong>{node.title}</strong></div></div><div className="node-tooltip-categories">{node.tags.map((tag) => <span key={tag}>{tag}</span>)}</div><p>{node.summary}</p>{node.rationale && <div className="node-tooltip-rationale"><b>Why</b><p>{node.rationale}</p></div>}<footer>{node.children.length} children · {node.links.length} outgoing references · depth {node.depth}</footer></aside>;
}

function GraphPanel({ document, selectedId, onSelect, onView, onOpenNode, relayoutToken, fitToken }: { document: MapDocument; selectedId?: string; onSelect(id: string): void; onView(view: MapDocument["view"]): void; onOpenNode(id: string): void; relayoutToken: number; fitToken: number }) {
  const all = flatten(document.nodes); const allTags = [...new Set(all.flatMap((node) => node.tags))].sort(); const filterTags = new Set(document.view.tag_filter_tags); const includeMode = document.view.tag_filter_mode === "include"; const nodes = visibleNodes(document).filter((node) => nodePassesTagFilter(node, document.view.tag_filter_mode, filterTags)); const radii = nodeRadii(all); const parents = new Map(all.map((node) => [node.id, node.parentId]));
  const [selected, setSelected] = useState<Set<string>>(() => new Set(selectedId ? [selectedId] : [])); const [zoom, setZoom] = useState(document.view.zoom || 0.7); const [pan, setPan] = useState<[number, number]>(document.view.viewport ?? [0, 0]); const [positions, setPositions] = useState<Record<string, [number, number]>>(() => ({ ...document.view.positions })); const [layoutRunning, setLayoutRunning] = useState(false);
  const svg = useRef<SVGSVGElement>(null); const drag = useRef<{ mode: "pan" | "nodes" | "marquee"; x: number; y: number; lastX: number; lastY: number; startPan: [number, number]; ids?: string[]; nodeId?: string; wasSelected?: boolean; activated?: boolean } | undefined>(undefined); const clickTimers = useRef(new Map<string, ReturnType<typeof setTimeout>>()); const [marquee, setMarquee] = useState<{ left: number; top: number; width: number; height: number }>(); const [tooltip, setTooltip] = useState<{ node: IndexedNode; x: number; y: number; hostWindow: Window }>();
  const point = useCallback((node: IndexedNode, index: number): [number, number] => positions[node.id] ?? [600 + Math.cos(index * 2.399963) * (100 + Math.sqrt(index) * 74), 400 + Math.sin(index * 2.399963) * (100 + Math.sqrt(index) * 74)], [positions]);
  const placed = nodes.map((node, index) => ({ node, point: point(node, index) })); const byId = new Map(placed.map((entry) => [entry.node.id, entry])); const visibleIds = new Set(byId.keys());
  const representative = (id: string) => { let value: string | undefined = id; while (value && !visibleIds.has(value)) value = parents.get(value); return value; }; const semantic = new Map<string, { source: string; target: string; relation: string }>(); for (const source of all) for (const link of source.links) { const a = representative(source.id), b = representative(link.target); if (a && b && a !== b) semantic.set(`${a}|${b}|${link.relation}`, { source: a, target: b, relation: link.relation }); }
  const commit = (p = positions, z = zoom, v = pan) => onView({ ...document.view, positions: { ...document.view.positions, ...p }, zoom: z, viewport: v });
  const fit = () => { const next = fitGraph(placed, radii); setZoom(next.zoom); setPan(next.viewport); commit(positions, next.zoom, next.viewport); };
  const relayout = () => { if (layoutRunning || nodes.length === 0) return; setLayoutRunning(true); const worker = new Worker(new URL("./layout.worker.ts", import.meta.url), { type: "module" }); const layoutNodes = nodes.map((node, index) => { const p = point(node, index); return { id: node.id, radius: radii.get(node.id) ?? 40, x: p[0], y: p[1] }; }); const ids = new Set(nodes.map((node) => node.id)); const links: Array<{ source: string; target: string; kind: "hierarchy" | "semantic" }> = []; for (const node of nodes) { if (node.parentId && ids.has(node.parentId)) links.push({ source: node.parentId, target: node.id, kind: "hierarchy" }); for (const link of node.links) if (ids.has(link.target)) links.push({ source: node.id, target: link.target, kind: "semantic" }); } worker.onmessage = (event: MessageEvent<Record<string, [number, number]>>) => { const next = event.data; setPositions(next); commit(next); setLayoutRunning(false); worker.terminate(); }; worker.onerror = () => { setLayoutRunning(false); worker.terminate(); }; worker.postMessage({ nodes: layoutNodes, links }); };
  useEffect(() => { if (relayoutToken > 0) relayout(); }, [relayoutToken]); useEffect(() => { if (fitToken > 0) queueMicrotask(fit); }, [fitToken]); useEffect(() => () => { for (const timer of clickTimers.current.values()) clearTimeout(timer); }, []);
  useEffect(() => { if (!drag.current) { setPositions({ ...document.view.positions }); setZoom(document.view.zoom); setPan(document.view.viewport); } }, [document.view.positions, document.view.zoom, document.view.viewport]);
  const worldPoint = (clientX: number, clientY: number): [number, number] => { const [sx, sy] = clientToViewBox(svg.current!, clientX, clientY); return [(sx - pan[0] - 600) / zoom + 600, (sy - pan[1] - 400) / zoom + 400]; };
  const toggleExpanded = (nodeId: string) => { const node = all.find((entry) => entry.id === nodeId); if (!node?.children.length) return; const expanded = new Set(document.view.expanded); expanded.has(nodeId) ? expanded.delete(nodeId) : expanded.add(nodeId); onView({ ...document.view, expanded: [...expanded] }); };
  const filterTag = (tag: string) => { const next = new Set(document.view.tag_filter_tags); next.has(tag) ? next.delete(tag) : next.add(tag); onView({ ...document.view, tag_filter_tags: [...next] }); };
  return <section className="graph-canvas" onContextMenu={(event) => event.preventDefault()} onWheel={(event) => { event.preventDefault(); const next = Math.max(0.2, Math.min(2.5, zoom * (event.deltaY < 0 ? 1.12 : 0.89))); setZoom(next); commit(positions, next, pan); }} onPointerDown={(event) => { if ((event.target as Element).closest("button, details")) return; if (event.button === 0) { const nodeId = (event.target as Element).closest<SVGGElement>("[data-node-id]")?.dataset.nodeId; setTooltip(undefined); event.currentTarget.setPointerCapture(event.pointerId); drag.current = { mode: "pan", x: event.clientX, y: event.clientY, lastX: event.clientX, lastY: event.clientY, startPan: pan, nodeId }; } else if (event.button === 2 && ((event.target as Element).tagName === "svg" || event.target === event.currentTarget)) { event.preventDefault(); setTooltip(undefined); setSelected(new Set()); event.currentTarget.setPointerCapture(event.pointerId); drag.current = { mode: "marquee", x: event.clientX, y: event.clientY, lastX: event.clientX, lastY: event.clientY, startPan: pan }; setMarquee({ left: event.clientX, top: event.clientY, width: 0, height: 0 }); } }} onPointerMove={(event) => { const state = drag.current; if (!state) return; if (state.mode === "pan") setPan([state.startPan[0] + event.clientX - state.x, state.startPan[1] + event.clientY - state.y]); else if (state.mode === "nodes") { if (!state.activated && Math.hypot(event.clientX - state.x, event.clientY - state.y) < 4) return; if (!state.activated) { state.activated = true; if (!state.wasSelected && state.nodeId) { state.ids = [state.nodeId]; setSelected(new Set(state.ids)); onSelect(state.nodeId); } } const [dx, dy] = screenDeltaToWorld(svg.current!, state.lastX, state.lastY, event.clientX, event.clientY, zoom); state.lastX = event.clientX; state.lastY = event.clientY; setPositions((current) => { const next = { ...current }; for (const id of state.ids ?? []) { const p = next[id] ?? byId.get(id)?.point ?? [0, 0]; next[id] = [p[0] + dx, p[1] + dy]; } return next; }); } else setMarquee({ left: Math.min(state.x, event.clientX), top: Math.min(state.y, event.clientY), width: Math.abs(event.clientX - state.x), height: Math.abs(event.clientY - state.y) }); }} onPointerUp={(event) => { const state = drag.current; if (state?.mode === "marquee") { const a = worldPoint(state.x, state.y), b = worldPoint(event.clientX, event.clientY); setSelected(new Set(placed.filter(({ point: p }) => p[0] >= Math.min(a[0], b[0]) && p[0] <= Math.max(a[0], b[0]) && p[1] >= Math.min(a[1], b[1]) && p[1] <= Math.max(a[1], b[1])).map(({ node }) => node.id))); } if (state?.mode === "pan") { const moved = Math.hypot(event.clientX - state.x, event.clientY - state.y); if (moved < 4) setPan(state.startPan); else setPan((value) => { commit(positions, zoom, value); return value; }); if (state.nodeId && moved < 4) { const pending = clickTimers.current.get(state.nodeId); if (pending) { clearTimeout(pending); clickTimers.current.delete(state.nodeId); toggleExpanded(state.nodeId); } else { const nodeId = state.nodeId; clickTimers.current.set(nodeId, setTimeout(() => { clickTimers.current.delete(nodeId); onOpenNode(nodeId); }, 280)); } } } if (state?.mode === "nodes") { if (state.activated) setPositions((value) => { commit(value); return value; }); else if (state.nodeId) { const next = new Set(selected); if (event.ctrlKey || event.shiftKey) next.has(state.nodeId) ? next.delete(state.nodeId) : next.add(state.nodeId); else next.add(state.nodeId); setSelected(next); if (next.has(state.nodeId)) onSelect(state.nodeId); } } drag.current = undefined; setMarquee(undefined); }}>
    <svg ref={svg} viewBox="0 0 1200 800"><defs><marker id="arrow" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="5" markerHeight="5" orient="auto"><path d="M0 0L10 5L0 10z" /></marker>{placed.map(({ node }) => <clipPath id={`clip-${node.id}`} key={node.id}><circle r={(radii.get(node.id) ?? 40) - 4} /></clipPath>)}</defs><g transform={`translate(${pan[0]} ${pan[1]}) translate(600 400) scale(${zoom}) translate(-600 -400)`}>
      {placed.map(({ node, point: p }) => node.parentId && byId.get(node.parentId) && <line key={`h-${node.id}`} x1={p[0]} y1={p[1]} x2={byId.get(node.parentId)!.point[0]} y2={byId.get(node.parentId)!.point[1]} className="graph-edge hierarchy" />)}{[...semantic.values()].map((edge) => { const a = byId.get(edge.source)!.point, b = byId.get(edge.target)!.point; return <g key={`${edge.source}-${edge.target}-${edge.relation}`}><line x1={a[0]} y1={a[1]} x2={b[0]} y2={b[1]} className="graph-edge semantic" markerEnd="url(#arrow)" />{zoom > .9 && <text x={(a[0] + b[0]) / 2} y={(a[1] + b[1]) / 2} className="edge-label">{edge.relation}</text>}</g>; })}
      {placed.map(({ node, point: p }) => { const radius = radii.get(node.id) ?? 40, lines = wrapTitle(node.title, radius), palette = nodePalette(node); return <g key={node.id} data-node-id={node.id} transform={`translate(${p[0]} ${p[1]})`} className={`graph-node ${selected.has(node.id) ? "selected" : ""}`} onPointerEnter={(event) => { if (!drag.current) setTooltip({ node, x: event.clientX, y: event.clientY, hostWindow: event.currentTarget.ownerDocument.defaultView! }); }} onPointerMove={(event) => { if (!drag.current) setTooltip({ node, x: event.clientX, y: event.clientY, hostWindow: event.currentTarget.ownerDocument.defaultView! }); }} onPointerLeave={() => setTooltip((current) => current?.node.id === node.id ? undefined : current)} onPointerDown={(event) => { if (event.button !== 2) return; event.preventDefault(); event.stopPropagation(); setTooltip(undefined); event.currentTarget.setPointerCapture(event.pointerId); const wasSelected = selected.has(node.id); drag.current = { mode: "nodes", x: event.clientX, y: event.clientY, lastX: event.clientX, lastY: event.clientY, startPan: pan, ids: wasSelected ? [...selected] : [node.id], nodeId: node.id, wasSelected, activated: false }; }}><circle r={radius} style={{ fill: palette.fill, stroke: palette.stroke, filter: palette.glow }} /><text clipPath={`url(#clip-${node.id})`} textAnchor="middle" y={-((lines.length - 1) * 7)}>{lines.map((line, index) => <tspan key={index} x="0" dy={index ? 14 : 0}>{line}</tspan>)}</text>{zoom >= .72 && <text clipPath={`url(#clip-${node.id})`} className="graph-node-type" textAnchor="middle" y={lines.length * 8 + 13}>{node.main_tag} · {node.children.length}</text>}</g>; })}
    </g></svg>{marquee && <div className="selection-marquee" style={marquee} />}{tooltip && <HoverTooltip {...tooltip} />}<details className="tag-filter" onPointerDown={(event) => event.stopPropagation()} onWheel={(event) => event.stopPropagation()}><summary>Tags · {includeMode ? "Include" : "Exclude"}{filterTags.size ? ` (${filterTags.size})` : ""}</summary><div><button className="tag-filter-mode" onClick={() => onView({ ...document.view, tag_filter_mode: includeMode ? "exclude" : "include" })}>{includeMode ? "Inclusive" : "Exclusive"}</button><button className="tag-filter-reset" onClick={() => onView({ ...document.view, tag_filter_tags: includeMode ? allTags : [] })}>Show all</button><button className="tag-filter-reset" onClick={() => onView({ ...document.view, tag_filter_tags: includeMode ? [] : allTags })}>Hide all</button>{allTags.map((tag) => <button key={tag} className={filterTags.has(tag) ? "active" : ""} onClick={() => filterTag(tag)}>{tag}</button>)}</div></details><div className="graph-controls"><button onClick={fit}>Fit</button><button onClick={relayout} disabled={layoutRunning}>{layoutRunning ? "Laying out…" : "Re-layout"}</button><button onClick={() => onView({ ...document.view, expanded: all.filter((node) => node.children.length).map((node) => node.id) })}>Show all nodes</button><button onClick={() => onView({ ...document.view, expanded: [] })}>Roots</button></div><div className="graph-hud"><strong>{document.project.name}</strong><span>{nodes.length}/{all.length} nodes · {selected.size} selected · {Math.round(zoom * 100)}%</span></div>
  </section>;
}

function Inspector({ node, allNodes, onApply, onSelectNode }: { node?: IndexedNode; allNodes: IndexedNode[]; onApply(values: Partial<MapNode>): void; onSelectNode(id: string): void }) {
  if (!node) return <Empty title="Inspector" text="Select a node in the graph, outline, or search results." />;
  return <section className="inspector-tooltip-shell"><header><span className="node-tooltip-swatch" style={{ background: nodePalette(node).stroke }} /><strong>{node.title}</strong></header><NodeEditor node={node} allNodes={allNodes} allTags={[...new Set(allNodes.flatMap((entry) => entry.tags))].sort()} onApply={onApply} onSelectNode={onSelectNode} /></section>;
}

function Outline({ nodes, selectedId, onSelect }: { nodes: IndexedNode[]; selectedId?: string; onSelect(id: string): void }) {
  const refs = useRef(new Map<string, HTMLButtonElement>()); useEffect(() => { if (selectedId) refs.current.get(selectedId)?.scrollIntoView({ block: "nearest", behavior: "smooth" }); }, [selectedId]);
  return <section className="panel-content"><p className="eyebrow">OUTLINE</p><div className="outline">{nodes.map((node) => <button ref={(element) => { if (element) refs.current.set(node.id, element); else refs.current.delete(node.id); }} key={node.id} className={selectedId === node.id ? "active" : ""} style={{ paddingLeft: `${10 + node.depth * 14}px` }} onClick={() => onSelect(node.id)}>{node.children.length ? "◆" : "·"} {node.title}</button>)}</div></section>;
}

function Search({ nodes, onSelect }: { nodes: IndexedNode[]; onSelect(id: string): void }) { const [query, setQuery] = useState(""); const results = query ? nodes.filter((node) => `${node.title} ${node.summary} ${node.tags.join(" ")}`.toLowerCase().includes(query.toLowerCase())).slice(0, 100) : []; return <section className="panel-content"><p className="eyebrow">SEARCH</p><input autoFocus placeholder="Search the map…" value={query} onChange={(event) => setQuery(event.target.value)} /><div className="search-results">{results.map((node) => <button key={node.id} onClick={() => onSelect(node.id)}><strong>{node.title}</strong><span>{node.tags.join(" · ")}</span></button>)}</div></section>; }
function Empty({ title, text }: { title: string; text: string }) { return <section className="panel-content"><p className="eyebrow">{title.toUpperCase()}</p><h2>{title}</h2><p>{text}</p></section>; }

function Panel({ id, document, selectedId, setSelected, applyDocument, activity, relayoutToken, fitToken, toggleNodeProperty }: { id: PanelId; document: MapDocument; selectedId?: string; setSelected(id: string): void; applyDocument(value: MapDocument, message?: string, immediate?: boolean): void; activity: string[]; relayoutToken: number; fitToken: number; toggleNodeProperty(id: string): void }) {
  const all = flatten(document.nodes); const selected = all.find((node) => node.id === selectedId); const applyNode = (nodeId: string, values: Partial<MapNode>) => { const node = all.find((entry) => entry.id === nodeId); applyDocument({ ...document, nodes: updateNode(document.nodes, nodeId, values) }, `Changed “${node?.title ?? "node"}”`, true); };
  if (id === "graph") return <GraphPanel document={document} selectedId={selectedId} onSelect={setSelected} onView={(view) => applyDocument({ ...document, view }, undefined, false)} onOpenNode={toggleNodeProperty} relayoutToken={relayoutToken} fitToken={fitToken} />;
  if (id === "inspector") return <Inspector node={selected} allNodes={all} onApply={(values) => selected && applyNode(selected.id, values)} onSelectNode={setSelected} />;
  if (id === "outline") return <Outline nodes={all} selectedId={selectedId} onSelect={setSelected} />;
  if (id === "search") return <Search nodes={all} onSelect={setSelected} />;
  if (id === "assistant") return <AssistantPanel />;
  return <section className="panel-content"><p className="eyebrow">ACTIVITY</p><div className="activity">{activity.length ? activity.map((entry, index) => <div key={`${entry}-${index}`}>{entry}</div>) : <p>No events in this session.</p>}</div></section>;
}

export function App() {
  const [document, setDocument] = useState<MapDocument>(emptyMap()); const documentRef = useRef(document); const [dockModel, setDockModel] = useState(() => ensureGraph(workspaceModel())); const [mapPath, setMapPath] = useState<string>(); const mapPathRef = useRef<string | undefined>(undefined); const [selectedId, setSelectedId] = useState<string>(); const [activity, setActivity] = useState<string[]>([]); const [relayoutToken, setRelayoutToken] = useState(0); const [fitToken, setFitToken] = useState(0); const undo = useRef<MapDocument[]>([]); const redo = useRef<MapDocument[]>([]); const saveTimer = useRef<ReturnType<typeof setTimeout> | undefined>(undefined); const pendingSave = useRef<MapDocument | undefined>(undefined); const saveChain = useRef(Promise.resolve()); const saveGeneration = useRef(0);
  const record = useCallback((message: string) => setActivity((items) => [`${new Date().toLocaleTimeString()}  ${message}`, ...items].slice(0, 250)), []);
  const enqueueSave = useCallback((value: MapDocument, message?: string) => { if (!mapPathRef.current) return; const generation = saveGeneration.current; saveChain.current = saveChain.current.then(async () => { if (generation !== saveGeneration.current) return; await window.mindmap.save(value); if (message) record(`Saved: ${message}`); }).catch((error) => record(`Save failed: ${String(error)}`)); }, [record]);
  const scheduleSave = useCallback((value: MapDocument, message?: string, immediate = false) => { pendingSave.current = value; if (saveTimer.current) clearTimeout(saveTimer.current); const flush = () => { const pending = pendingSave.current; pendingSave.current = undefined; if (pending) enqueueSave(pending, message); }; if (immediate) flush(); else saveTimer.current = setTimeout(flush, 120); }, [enqueueSave]);
  const applyDocument = useCallback((value: MapDocument, message?: string, immediate = false) => { const current = documentRef.current; if (value !== current) { undo.current.push(current); if (undo.current.length > 100) undo.current.shift(); redo.current = []; } documentRef.current = value; setDocument(value); if (message) record(`User: ${message}`); scheduleSave(value, message, immediate); }, [record, scheduleSave]);
  const load = useCallback((result: { path: string; data: unknown }, source = "Opened") => { const incoming = result.data as MapDocument; const fitted = { ...incoming, view: { ...incoming.view, zoom: 0.7, viewport: [0, 0] as [number, number] } }; documentRef.current = fitted; setDocument(fitted); mapPathRef.current = result.path; setMapPath(result.path); setSelectedId(undefined); undo.current = []; redo.current = []; const model = ensureGraph(workspaceModel(fitted.view.workspace)); setDockModel(model); setFitToken((value) => value + 1); record(`${source}: ${result.path}`); }, [record]);
  useEffect(() => { void window.mindmap.loadDefault().then((result) => load(result, "Opened project")).catch((error) => record(`Open failed: ${String(error)}`)); return window.mindmap.onExternalChange((result) => { if (result.path !== mapPathRef.current) return; saveGeneration.current += 1; if (saveTimer.current) clearTimeout(saveTimer.current); pendingSave.current = undefined; const incoming = result.data as MapDocument; const currentWorkspace = JSON.stringify(documentRef.current.view.workspace); documentRef.current = incoming; setDocument(incoming); if (JSON.stringify(incoming.view.workspace) !== currentWorkspace) setDockModel(ensureGraph(workspaceModel(incoming.view.workspace))); record(result.source === "assistant" ? "AI Assistant updated the project file" : "External change loaded from project file"); }); }, [load, record]);
  const openPanel = useCallback((panel: PanelId) => { const tabId = `panel-${panel}`; if (dockModel.getNodeById(tabId)) { dockModel.doAction(Actions.selectTab(tabId)); return; } const destination = dockModel.getActiveTabset() ?? dockModel.getFirstTabSet(); dockModel.doAction(Actions.addNode({ type: "tab", id: tabId, name: panelLabel[panel], component: panel, enablePopout: true }, destination.getId(), DockLocation.CENTER, -1, true)); }, [dockModel]);
  const toggleNodeProperty = useCallback((nodeId: string) => { const tabId = `node-property-${nodeId}`, existing = dockModel.getNodeById(tabId); if (existing) { dockModel.doAction(Actions.deleteTab(tabId)); return; } const node = flatten(documentRef.current.nodes).find((entry) => entry.id === nodeId); if (!node) return; const destination = dockModel.getNodeById("inspector-tabset") ?? dockModel.getActiveTabset() ?? dockModel.getFirstTabSet(); if (!destination) return; dockModel.doAction(Actions.addNode({ type: "tab", id: tabId, name: `Node Property · ${node.title}`, component: "node-property", config: { nodeId }, enablePopout: true }, destination.getId(), DockLocation.CENTER, -1, true)); }, [dockModel]);
  const addNode = useCallback((parent?: string) => { const current = documentRef.current, node = createNode(current); const next = { ...current, nodes: insertNode(current.nodes, node, parent) }; setSelectedId(node.id); applyDocument(next, `Created “${node.title}”`, true); }, [applyDocument]);
  const deleteSelected = useCallback(() => { if (!selectedId) return; const current = documentRef.current, selected = flatten(current.nodes).find((node) => node.id === selectedId); if (!selected || !confirm(`PERMANENT DELETION\n\nDelete “${selected.title}” and its complete subtree?\n\nThis is saved to the project file immediately and cannot be undone.`)) return; const removed = removeNode(current.nodes, selectedId), positions = { ...current.view.positions }, expanded = current.view.expanded.filter((id) => !removed.removed.has(id)); for (const id of removed.removed) positions[id] && delete positions[id]; setSelectedId(undefined); applyDocument({ ...current, nodes: removed.nodes, view: { ...current.view, positions, expanded } }, `Permanently deleted “${selected.title}” and ${removed.removed.size - 1} descendants`, true); undo.current = []; redo.current = []; }, [applyDocument, selectedId]);
  const saveAs = useCallback(async () => { const saved = await window.mindmap.saveAs(documentRef.current); if (saved) { mapPathRef.current = saved; setMapPath(saved); record(`Saved project as ${saved}`); } }, [record]);
  useEffect(() => window.mindmap.onCommand((command) => { const current = documentRef.current; if (command === "open") void window.mindmap.open().then((result) => result && load(result)); else if (command === "new") void (async () => { const next = emptyMap(), saved = await window.mindmap.saveAs(next); if (saved) load({ path: saved, data: next }, "Created project"); })(); else if (command === "save") enqueueSave(current, "manual save"); else if (command === "save-as") void saveAs(); else if (command === "undo" && undo.current.length) { const previous = undo.current.pop()!; redo.current.push(current); documentRef.current = previous; setDocument(previous); scheduleSave(previous, "undo", true); record("User: undo"); } else if (command === "redo" && redo.current.length) { const next = redo.current.pop()!; undo.current.push(current); documentRef.current = next; setDocument(next); scheduleSave(next, "redo", true); record("User: redo"); } else if (command === "add-root") addNode(); else if (command === "add-child" && selectedId) addNode(selectedId); else if (command === "delete-selected") deleteSelected(); else if (command.startsWith("show-panel:")) { const panel = command.slice(11) as PanelId; if (PANEL_IDS.includes(panel)) openPanel(panel); } else if (command === "reset-workspace") { const model = ensureGraph(workspaceModel()); setDockModel(model); applyDocument({ ...current, view: { ...current.view, workspace: model.toJson() } }, "Reset panel arrangement", true); } else if (command === "fit-graph") setFitToken((value) => value + 1); else if (command === "relayout-graph") setRelayoutToken((value) => value + 1); else if (command === "show-all") applyDocument({ ...current, view: { ...current.view, expanded: flatten(current.nodes).filter((node) => node.children.length).map((node) => node.id) } }, "Showed all nodes"); else if (command === "collapse-all") applyDocument({ ...current, view: { ...current.view, expanded: [] } }, "Collapsed to roots"); else if ((command === "expand-selected" || command === "collapse-selected") && selectedId) { const expanded = new Set(current.view.expanded); command === "expand-selected" ? expanded.add(selectedId) : expanded.delete(selectedId); applyDocument({ ...current, view: { ...current.view, expanded: [...expanded] } }); } }), [addNode, applyDocument, deleteSelected, enqueueSave, load, openPanel, record, saveAs, scheduleSave, selectedId]);
  const selectNode = (id: string) => { setSelectedId(id); openPanel("inspector"); };
  const panel = (id: PanelId) => <Panel id={id} document={document} selectedId={selectedId} setSelected={selectNode} applyDocument={applyDocument} activity={activity} relayoutToken={relayoutToken} fitToken={fitToken} toggleNodeProperty={toggleNodeProperty} />;
  const nodePanel = (nodeId: string) => { const all = flatten(document.nodes), node = all.find((entry) => entry.id === nodeId); if (!node) return <Empty title="Node unavailable" text="This node no longer exists in the current project file." />; return <section className="inspector-tooltip-shell docked-node-editor"><header><span className="node-tooltip-swatch" style={{ background: nodePalette(node).stroke }} /><strong>{node.title}</strong></header><NodeEditor node={node} allNodes={all} allTags={[...new Set(all.flatMap((entry) => entry.tags))].sort()} onSelectNode={selectNode} onApply={(values) => applyDocument({ ...documentRef.current, nodes: updateNode(documentRef.current.nodes, node.id, values) }, `Changed “${node.title}”`, true)} /></section>; };
  return <main className="app-shell"><header className="workspace-header"><div className="workspace-title"><span className="brand-mark" /><span>{document.project.name}</span></div><div className="workspace-actions">{PANEL_IDS.map((id) => <button key={id} onClick={() => openPanel(id)}>{panelLabel[id]}</button>)}</div></header><section className="workspace-layout"><Layout model={dockModel} supportsPopout popoutURL="popout.html" popoutWindowName="Project Knowledge Map" factory={(node: TabNode) => node.getComponent() === "node-property" ? nodePanel(String((node.getConfig() as { nodeId?: string } | undefined)?.nodeId ?? "")) : panel(node.getComponent() as PanelId)} onModelChange={(model) => { const current = documentRef.current, workspace = model.toJson(); if (JSON.stringify(current.view.workspace) === JSON.stringify(workspace)) return; applyDocument({ ...current, view: { ...current.view, workspace } }, undefined, false); }} /></section><footer className="workspace-status"><span>{mapPath ?? "No project file"}</span><span>{flatten(document.nodes).length} nodes · autosave active</span></footer></main>;
}
